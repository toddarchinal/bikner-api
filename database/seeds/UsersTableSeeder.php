<?php

use App\User;
use Illuminate\Database\Seeder;

class UsersTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        /*
        $user = new User;
        $user->name = "Admin";
        $user->email = "admin@bikner.com";
        $user->password = bcrypt('harley');
        $user->is_admin = true;
        $user->save();

        $user = new User;
        $user->name = "Todd";
        $user->email = "toddarchinal@gmail.com";
        $user->password = bcrypt('harley');
        $user->is_admin = false;
        $user->save();
        */
    }
}
